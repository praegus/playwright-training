// String
const firstName: string = "Test";
const lastName = "User";
const middleName = 'bla'

const fullName_long = firstName + " " + middleName + " " + lastName
const fullName = `${firstName} ${middleName} ${lastName}`

console.log('my full name is: ' + fullName_long)
console.log(`my full name is: ${fullName}`)